/*
#define light 2 //the little rays output light
#define is_dark 1 //input from transistor- high = "TURN ON LIGHT!!!"
void setup() {
  //Setting up
  pinMode(light, OUTPUT);
  pinMode(is_dark, INPUT);
}

void loop() {
  if(digitalRead(is_dark)==HIGH){
    digitalWrite(light, HIGH);
  }else{
    digitalWrite(light, LOW);
  }
}
 */
